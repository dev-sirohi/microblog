Base -
    RefreshToken(with cred)

Login
    - loginuser(loginrequest)

Signup
    - signupuser(signuprequest)

Home
    - gethomefeed

Post {Post + TopComments}

CommentSection {postId}
    - getcommentsbypostid(postId)
    - addcomment(postId)

AddPost
    - createPost
    - saveMediaFile
    - redirect to home where once your post is created it is pushed to the top of the stack of posts so you can see your own post

Profile {userId}
    - getUserProfile(userId)
    - getuserrecentlylikedposts

MyProfile
    - getUserProfile
    - getuserrecentlylikedposts
    - logout user

EditProfile
    - updateUserProfile(updateUserProfileRequest)